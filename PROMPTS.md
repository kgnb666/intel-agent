# 分阶段执行提示词

使用方法：每次开新会话，把【公共前缀】+ 对应阶段的提示词一起发给 agent。每个提示词都是自包含的，agent 无需看过之前的对话。

---

## 【公共前缀】（每次必带）

```
我在做一个面试展示项目「智能行业情报分析 Agent」，位于 C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\yp\intel-agent\。

项目背景：自动化行业情报流水线——采集行业资讯 → 清洗去重 → LLM 分析 → 可视化日报 → 邮件推送。行业主题为"电商与消费"，关键词在 config.yaml 中配置。

当前已完成：
- config.yaml：行业关键词、RSS 数据源（36氪/IT之家/少数派）、采集参数
- src/crawler.py：RSS 抓取 + 关键词过滤 + 标题相似度去重（SequenceMatcher）
- src/storage.py：SQLite 存储，articles 表含 url/title/summary/source/published/fetched_at/analyzed 字段，url 唯一约束
- run_crawl.py：采集入口，已实测可用

环境约定（重要）：
- Python 解释器必须使用：C:\Users\kgnb666.DESKTOP-55DCFH3.003\.workbuddy\binaries\python\envs\default\Scripts\python.exe
- 新依赖安装到该 venv，不要全局安装；装完同步写进 requirements.txt
- 所有代码文件 UTF-8 编码，中文注释适度即可
- 每完成一个模块必须实际运行验证，不允许只写代码不测试

代码风格：简洁、可读、有明确函数边界。这是面试项目，面试官会读代码，请保持注释解释"为什么这么做"而不是"做了什么"。
```

---

## 阶段 1：LLM 分析模块（第 6-8 天）

```
【任务】为项目新增 LLM 分析模块，对库内未分析的文章（analyzed=0）调用大模型生成结构化分析。

要求：
1. 新建 src/analyzer.py：
   - 调用 OpenAI 兼容 API（默认 base_url 用 DeepSeek: https://api.deepseek.com，模型 deepseek-chat；base_url/model/api_key 都从环境变量或 config.yaml 读取，API key 绝不硬编码）
   - 对每篇文章输出 JSON：摘要（80字内）、情感倾向（正面/中性/负面+置信度）、行业标签、关键实体（公司/产品）
   - 用 prompt 强制模型只返回合法 JSON，解析失败要重试（最多 2 次）并记录失败
2. 新建 analysis 结果表（或扩展 articles 表），存储分析结果
3. 成本控制：批量处理时每批最多 20 篇，支持限制单次运行最大处理数（config 可配）
4. 容错：API 超时/限流时指数退避重试；单篇失败不阻断整批
5. 新建 run_analyze.py 作为入口，运行后打印：处理数、成功数、失败数、消耗 token 数
6. 我没有 API key 时，代码必须能以 --dry-run 模式运行（打印将要发送的 prompt 而不实际调用），方便我先验证逻辑

完成后：更新 README 的 Roadmap，并实际运行验证（dry-run 模式即可）。
```

---

## 阶段 2：RAG 历史情报关联（第 8-9 天）

```
【任务】为项目新增 RAG 模块，让每篇新情报能关联历史相似事件，形成"趋势脉络"。这是面试核心亮点，要能讲清楚为什么用 RAG。

要求：
1. 新建 src/rag.py：
   - 用 embedding API（与 analyzer 同一套 OpenAI 兼容配置，DeepSeek 无 embedding 则改用硅基流动免费模型 BAAI/bge-m3，base_url: https://api.siliconflow.cn/v1）
   - 文章摘要做向量化，存入 SQLite（BLOB 存 float 数组即可，数据量小不需要 faiss，用 numpy 算余弦相似度）
   - 每篇新文章检索 Top-3 历史相似文章，相似度 ≥ 0.6 才判定为"相关事件"（bge-m3 语义相似度偏低，0.6 为质量/数量平衡点）
2. 关联结果入库：related_event 字段记录关联链条（如"拼多多 Q2 财报"关联"拼多多 Q1 财报"）
3. 趋势提炼：每周对相似事件聚类，让 LLM 生成"本周趋势脉络"段落
4. 新建 run_rag.py 入口，输出示例关联结果供验证
5. 同样支持 --dry-run，无 key 时可验证流程

面试话术素材请在 README 写明：为什么不用关键词匹配而用向量检索（语义相似 vs 字面相似）、为什么数据量小不用向量数据库（过度工程）。
```

---

## 阶段 3：Streamlit 可视化看板（第 9-11 天）

```
【任务】新建 dashboard.py，用 Streamlit 做情报看板，这是面试演示的"脸面"，要好看、信息密度高。

页面结构：
1. 顶部指标卡：今日新增情报数、库内总量、本周正面/负面占比、已生成报告数
2. 情感趋势折线图：按日期统计正面/中性/负面数量（用 plotly 或 streamlit 原生图表，涨红跌绿的中国惯例不适用此处，情感用绿=正面/灰=中性/红=负面）
3. 情报列表：可按来源/情感/关键词筛选，每条展示标题+AI摘要+情感标签+关联历史事件
4. 趋势脉络板块：展示 RAG 聚类出的本周趋势
5. 侧边栏：行业关键词查看、数据时间范围选择器

交互能力（重点，面试演示的核心亮点）：
6. 对话式问答 Tab（用 st.chat_message / st.chat_input 实现）：
   - 用户用自然语言提问（如"这周拼多多有什么动态"），系统先向量检索库内相关情报（复用 src/rag.py 的检索逻辑），再把检索结果作为上下文交给 LLM 生成回答
   - 回答必须附引用来源（标题+链接），可点击跳转原文
   - 支持多轮对话，保留会话历史
   - 无 API key 时降级为纯检索模式：直接返回相关情报列表，界面标注"未配置 LLM"
7. 侧边栏"立即采集"按钮：现场触发一轮 run_crawl，显示采集结果统计（面试时演示实时数据流入）
8. 关键词在线配置：侧边栏可编辑关注关键词，保存后写回 config.yaml 立即生效

要求：
- 问答和看板从 SQLite 读取，除"立即采集"和"关键词配置"外只读不写
- 页面配色克制专业（白底、单强调色），不要花哨
- 运行验证：streamlit run dashboard.py 能正常打开，问答、筛选、一键采集逐项实测无报错
- README 补充看板截图位置的占位说明
```

---

## 阶段 4：邮件定时推送（第 11-12 天）

```
【任务】实现每日情报日报的生成与邮件推送。

要求：
1. 新建 src/report.py：
   - 汇总当日/昨日数据：新增情报数、Top 5 重要事件（按 LLM 判断的重要性排序）、情感分布、趋势脉络
   - 生成 HTML 格式邮件正文（邮件客户端兼容性优先，用 table 布局 + 内联样式，不用 flex/grid）
2. 新建 src/mailer.py：SMTP 发信（QQ 邮箱/163 邮箱 SMTP 配置从环境变量读取：SMTP_HOST/PORT/USER/PASSWORD/TO）
3. 新建 run_daily.py：一键执行完整流水线（采集 → 分析 → RAG 关联 → 生成报告 → 发邮件），每步打日志，某步失败邮件中注明降级结果
4. 定时方案：提供 Windows 任务计划程序的配置说明（schtasks 命令），写进 README
5. 测试：用 --no-send 模式生成 HTML 报告文件到 output/ 目录，我直接在浏览器预览效果
```

---

## 阶段 5：打磨与上线（第 12-14 天）

```
【任务】项目收尾，目标是"面试官打开链接就能看，读 README 就信服"。

要求：
1. README 重构：项目动机 → 架构图（用文字或 mermaid）→ 技术决策（为什么用 RSS 而非爬虫为主、为什么 SQLite 够用、双重去重设计、RAG 选型理由）→ 量化成果（处理量、去重率、报告生成耗时）→ 演示截图
2. 代码审查：给每个模块的 docstring 补齐，删除调试残留，统一命名风格
3. 部署：Streamlit 看板部署到 Streamlit Community Cloud（免费），数据库用预置的 3 天数据快照；写清楚部署步骤
4. 生成一份「面试问答预案.md」：预测 10 个面试官可能问的技术问题+回答要点（如：数据量大了怎么办、怎么防反爬、LLM 输出不稳定怎么处理、怎么评估摘要质量）
5. 最后完整跑一遍全流程，确认从零 clone 到跑通的步骤文档准确
```

---

## 阶段 0（可选）：采集模块回归验证

```
【任务】对现有采集模块做一次健壮性强化：
1. 为 src/crawler.py 的关键词过滤、相似度去重写 pytest 单元测试（至少 6 个用例）
2. 增加重试机制：RSS 源请求失败时重试 2 次，间隔 5 秒
3. 运行测试确保全绿，并跑一轮真实采集确认无回归
```
