# 面试问答预案

针对「智能行业情报分析 Agent」项目，预测面试官最可能追问的 10 个技术问题及回答要点。

---

**Q1：数据量大了怎么办？现在全量算相似度不会慢吗？**

分层回答：
- 采集层：URL 唯一约束 + 增量抓取，入库量本身就是收敛的
- 向量检索层：当前百级数据 numpy 全量扫描毫秒级；到万级引入 ANN 索引（faiss/hnswlib），十万级以上再考虑专业向量库
- 存储层：SQLite 到十万级没问题，更大迁移 PostgreSQL，只需改 `storage.py` 连接层——因为我把存储访问收敛在了一个类里
- 要点：承认当前选型的边界，同时说明架构上预留了迁移路径（分层、接口收敛）

**Q2：怎么防反爬？**

主力数据源选 RSS 就是为了绕开这个问题：RSS 是官方开放的订阅接口，稳定、无反爬、结构规范。补充手段才考虑爬虫，届时用 UA 池、请求间隔、代理池。另外 RSS 请求也做了超时 + 重试 2 次的容错。要点：最好的反爬对抗是不对抗——优先找官方数据源。

**Q3：LLM 输出不稳定，JSON 解析失败怎么办？**

四层防御：
1. prompt 里写死字段契约（字段名、枚举值、数值范围、字数限制）
2. API 层用 `response_format=json_object` 强制
3. 解析端"宽容提取 + 严格校验"：正则取第一个 JSON 对象，再校验字段完整性、情感枚举合法性、置信度截断到 [0,1]
4. 失败重试最多 2 次，仍失败记录留痕、跳过该篇不阻断整批
要点：不能把 LLM 当确定性函数用，要用工程手段兜底。

**Q4：怎么评估摘要质量？**

- 低成本方案：人工抽检打分（相关性/忠实度/简洁度），样本量 30~50 条
- 自动化方案：用更强的模型当裁判（LLM-as-judge），对比摘要与原文的事实一致性
- 指标方案：ROUGE 对参考摘要在本场景不适用（没有标注参考），更实际的是"事实性错误率"
- 要点：先明确"什么是好摘要"的业务定义，再选评估手段；项目里我留的是人工抽检 + 置信度过滤

**Q5：为什么不用关键词匹配做历史关联，非要用向量检索？**

举一个项目里的真实例子："拼多多财报超预期"和"多多买菜盈利改善"，字面上零交集，关键词匹配完全无能为力，但它们明显属于同一业务线的演进。embedding 把文本映射到语义空间，余弦相似度度量的是"语义距离"而非"字面重合"。要点：语义相似 ≠ 字面相似，这是 RAG 的核心价值。

**Q6：RAG 里为什么不用 faiss / 向量数据库？**

当前数据量（百级）下 numpy 全量算余弦是毫秒级，faiss 的优势要在十万级以上才体现。引入向量数据库意味着额外的服务部署、运维和依赖——对一个日报项目是过度工程。embedding 用 float32 BLOB 存 SQLite，零新增基础设施。要点：技术选型要匹配当前规模，架构预留升级路径即可（`rag.py` 里检索逻辑独立，换后端只改一处）。

**Q7：情感分析的置信度怎么用？**

两个用途：一是低置信度结果可以进人工复核队列（项目里目前只做展示）；二是日报 Top 事件排序时用置信度加权——情绪波动大且置信度高的事件排前面，中性资讯权重打折（×0.3）。要点：置信度不是摆设，要进入下游决策。

**Q8：API 成本怎么控制？**

- 单次运行硬性上限 `max_per_run=20` 篇，配置可调
- 分析过的文章打 `analyzed` 标记，增量处理不重复消费
- 每篇消耗 token 入库（`tokens_used` 字段），可追溯每日成本
- embedding 用硅基流动免费模型（bge-small-zh），向量环节零成本
要点：成本意识体现在"限额 + 增量 + 可观测"三件事上。

**Q9：如果让你继续做，下一步做什么？**

- 信源扩展：接入微信公众号/微博/雪球等，需要对付反爬
- 事件级聚合：现在去重到"相似标题"，下一步是把相似事件真正合并成"事件实体"，形成事件时间线
- 告警通道：负面高置信度事件实时推微信/钉钉 webhook，而不是等日报
- 要点：展示你对产品形态的思考，而不只是技术堆砌

**Q10：项目里最难的地方是什么？**

建议答：让 LLM 的输出"工程可用"。写 prompt 调出一个好看的 demo 很容易，但要做到字段稳定、异常可恢复、失败可观测、成本可控制，靠的是解析容错、重试退避、增量标记、降级策略这些不性感但必须的工程细节。要点：把答案落在"demo 和产品的差距"上，这是面试官想听到的认知。

---

## 演示动线建议（3 分钟版）

1. 打开看板 → 指指标卡和情感趋势图（30 秒讲整体）
2. 点侧边栏「立即采集」→ 现场跑一轮，展示数据实时流入（30 秒）
3. 切问答 Tab → 问"这周拼多多有什么动态"，展示带引用的回答（1 分钟）
4. 打开收到的日报邮件（或 output/ 里的 HTML）（30 秒）
5. 收尾讲架构图和技术决策（30 秒）
